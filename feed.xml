<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://rahulvigneswaran.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://rahulvigneswaran.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2024-02-08T14:11:48+00:00</updated><id>https://rahulvigneswaran.github.io/feed.xml</id><title type="html">blank</title><entry><title type="html">Constrained optimization and KKT Conditions</title><link href="https://rahulvigneswaran.github.io/blog/2024/kkt/" rel="alternate" type="text/html" title="Constrained optimization and KKT Conditions"/><published>2024-02-08T00:00:00+00:00</published><updated>2024-02-08T00:00:00+00:00</updated><id>https://rahulvigneswaran.github.io/blog/2024/kkt</id><content type="html" xml:base="https://rahulvigneswaran.github.io/blog/2024/kkt/"><![CDATA[<h2 id="necessity-vs-sufficiency">Necessity Vs Sufficiency</h2> <p>Proving something requires proving both the necessity and sufficiency. So lets first understand what they mean.</p> <p>Lets take \(A\)and \(B\).</p> <h3 id="necessity-a-implies-b">Necessity: \(A \implies B\)</h3> <ul> <li>Assume A is true</li> <li>Now show B is true as a consequence of the above assumption.</li> <li>When we say A is necessary for B, we mean that for B to be true, A must be true.</li> </ul> <h3 id="sufficiency-a-impliedby-b">Sufficiency: \(A \impliedby B\)</h3> <ul> <li>Assume B is true.</li> <li>Now show A is true as a consequence of B.</li> <li>When we say A is sufficient for B, we mean that for A to be true, B must be true.</li> </ul> <h3 id="necessary-and-sufficient-a-iff-b">Necessary and sufficient: \(A \iff B\)</h3> <ul> <li>If A is true, B is definitely true.</li> <li>If B is true, A is definitely true.</li> </ul> <p>For more check, <a href="https://www.ncl.ac.uk/webtemplate/ask-assets/external]maths-resources/economics/sets-and-logic/necessity-and-sufficiency.html">Source 1</a> and <a href="https://www.sfu.ca/~swartz/conditions1.htm">Source 2</a>.</p> <hr/> <h2 id="primal--dual">Primal &amp; Dual</h2> <p>Lets first establish some common notations before we prove anything The unconstrained optimisation problem is,</p> \[\min_x\;f(x)\] \[s.t.\; h_{i(x)}\leq 0, \forall i \quad [eqn (4)]\] \[l_{j(x)}= 0, \forall j \quad [eqn (3)]\] <p>This above is our primal.</p> <p>The dual for it is,</p> \[\max_{u,v}\;\min_{x\quad}f(x)+\sum\limits_{i=1}^mu_ih_i(x)+\sum\limits_{j=1}^rv_jl_j(x)\] \[s.t.\quad u\geq0 \quad [eqn (5)]\] <p>For the sake of simplicity lets define,</p> <p>Lagrangian: \(L(x,u,v)=f(x)+\sum\limits_{i=1}^mu_ih_i(x)+\sum\limits_{j=1}^rv_jl_j(x) \quad [eqn (1)]\)</p> \[g(u,v)=\min_x\;L(x,u,v) \quad [eqn (2)]\] <p>Dual: \(\max_{u,v}\; g(u,v)\)</p> <p>Solution of primal : \(x^{*}\)</p> <p>Solution of dual : \(u^{*}, v^{*}\)</p> <hr/> <h2 id="kkt-conditions">KKT Conditions</h2> <p>The KKT conditions are as follows,</p> <ol> <li>Stationary condition</li> </ol> \[0\in\partial\left(f(x)+\sum\limits_{i=1}^mu_ih_i(x)+\sum\limits_{j=1}^rv_jl_j(x)\right)\] <ol> <li> <p>Complementary slackness</p> \[u_{i}.h_{i}(x)=0,\quad \forall i\in {1,\dots,m}\] <blockquote> <p>Why is there no complementary slackness for \(v_jl_j\)? Because it is a equality constraint. At optimal \(x\) it will always be \(0\).</p> </blockquote> </li> <li> <p>Primal feasibility</p> \[h_{i(x)}\leq 0, \forall i\in {1,\dots,m}\] \[l_{j(x)}= 0, \forall j\in {1,\dots,r}\] </li> <li> <p>Dual feasibility</p> \[u_{i(x)}\geq 0, \forall i\in {1,\dots,m}\] <blockquote> <p>Why is there no dual feasibility for \(v_j\)? I dont know the answer yet. Will update once I understand it.</p> </blockquote> </li> </ol> <p>We are now going to prove \(\text{Optimality} \iff \text{KKT}\). To do that, like we saw earlier we need to prove both necessity and sufficiency.</p> <hr/> <h2 id="necessity----textoptimality-implies-textkkt">Necessity : \(\text{Optimality} \implies \text{KKT}\)</h2> <p>We are also assuming strong duality (Look into slater’s condition) and as a result there is no duality gap, that is \(f(x^{*})-g(u^{*}, v^{*})=0\)</p> <p>Lets first not worry about the KKT at all and solve as if we are solving a generic equation.</p> \[f(x^{*})= g(u^{*}, v^{*})\] \[= \min_{x}\;f(x)+\sum\limits_{i=1}^mu^{*}_ih_i(x)+\sum\limits_{j=1}^rv^{*}_jl_j(x)\] \[\leq f(x^*)+\sum\limits_{i=1}^mu^{*}_ih_i(x^*)+\sum\limits_{j=1}^rv^{*}_jl_j(x^*)\] \[\leq f(x^*)\] <p>Lets go through the above now step by step.</p> <ul> <li>Step 1: \(f(x^{*})= g(u^{*}, v^{*})\) <ul> <li>This is true because there is no duality gap.</li> </ul> </li> <li>Step 2: \(= \min_{x}\; f(x)+\sum\limits_{i=1}^mu^{*}_ih_i(x)+\sum\limits_{j=1}^rv^{*}_jl_j(x)\) <ul> <li>The above is by eqn (1) and eqn (2). We have already defined it. There is nothing special here.</li> </ul> </li> <li>Step 3: \(\leq f(x^*)+\sum\limits_{i=1}^mu^{*}_ih_i(x^*)+\sum\limits_{j=1}^rv^{*}_jl_j(x^*)\) <ul> <li>Lets understand how we went from step 2 to step 3. \(x^*\) is a solution for the primal but for \(g\) it is just some solution. There is no guarantee that it is optimal. Therefore it must be greater than equal to the actual optimal solution of \(f(x)+\sum\limits_{i=1}^mu^{*}_ih_i(x)+\sum\limits_{j=1}^rv^{*}_jl_j(x)\).</li> </ul> </li> <li>Step 4:\(\leq f(x^*)\) <ul> <li>Lets understand how we went from step 3 to step 4. Its is two parted. <ul> <li>Lets first focus on \(\sum\limits_{j=1}^rv^{*}_jl_j(x)\). <ul> <li>We already know that from eqn (3) that \(v^{*}_jl_j(x^*)\) must be 0 if \(x^*\) is the optimal if the constraints are respected. So summation of all individual \(j\)-s will also be 0. Therefore the term entirely will be zero.</li> </ul> </li> <li>Now lets focus on \(\sum\limits_{i=1}^mu^{*}_ih_i(x)\) <ul> <li>From eqn (4) we know that \(h_i(x)\) must be -ve. Then from eqn (5) we know that \(u^{*}_i\) must be +ve. As a result \(u^{*}_ih_i(x)\) is +ve multiplied by -ve, should be -ve too. When we sum up all $i$ which are individually negative values, the resulting summation would be -ve too.</li> </ul> </li> <li>Now lets regroup. (something) + (-ve value) + (zero) \(\leq\) (something). Therefore, \(f(x^*)+\sum\limits_{i=1}^mu^{*}_ih_i(x^*)+\sum\limits_{j=1}^rv^{*}_jl_{j(x^{*)}\leq}f(x^*)\).</li> </ul> </li> </ul> </li> </ul> <p>Now to prove necessity we have to prove that the inequalities in step 3 and step 4 are actually equalities. Now to do that we need the KKT conditions.</p> <ul> <li>\(\leq\) in step 4 <ul> <li>Lets first handle the inequality in step 4 as it is much easier to understand.</li> <li> <p>We already saw how we transitioned from step 3 to step 4. Now lets see how we can make the inequality into an equality.</p> <p>\(f(x^*)+\sum\limits_{i=1}^mu^{*}_ih_i(x^*)+\sum\limits_{j=1}^rv^{*}_jl_{j(x^{*)}\leq}f(x^*)\)</p> <ul> <li>\(\sum\limits_{j=1}^rv^{*}_jl_{j(x^{*})}\): <ul> <li>We have already shown that this term is 0. So we dont have to worry about it.</li> </ul> </li> <li>\(\sum\limits_{i=1}^mu^{*}_ih_i(x^*)\): <ul> <li>Now we have to show that this term is 0 in order for the inequality to become an equality. But we saw earlier that this term is actually -ve and not 0. So now comes the KKT condition 2 - The complimentary slackness condition. This enforces that \(u_{i}.h_{i}(x)=0\) . Since each term is 0, the summation entirely is 0. This makes the inequality into an equality. Without the KKT condition 2 this would have not been possible.</li> </ul> </li> </ul> </li> </ul> </li> <li>\(\leq\) in step 3 <ul> <li> <p>Now lets handle the only inequality left. This was slightly difficult for me to come to terms with.</p> </li> <li> \[\min_{x}\;f(x)+\sum\limits_{i=1}^mu^{*}_ih_i(x)+\sum\limits_{j=1}^rv^{*}_jl_j(x)\leq f(x^*)+\sum\limits_{i=1}^mu^{*}_ih_i(x^*)+\sum\limits_{j=1}^rv^{*}_jl_j(x^*)\] <ul> <li>We already saw that \(x^*\) is actually not an optimal solution for the LHS (that is \(g(x)\)) but is just some solution. So for the inequality to become an equality we have to somehow enforce that $x^*$ is also an optimal solution for \(g(x)\) too. <ul> <li>Lets focus only on the LHS : \(\min_{x}\;f(x)+\sum\limits_{i=1}^mu^{*}_ih_i(x)+\sum\limits_{j=1}^rv^{*}_jl_j(x)\) <ul> <li>Note that from eqn (1), \(L(x,u^{*},v^{*})=f(x)+\sum\limits_{i=1}^mu^{*}_ih_i(x)+\sum\limits_{j=1}^rv^{*}_jl_j(x)\) . So the LHS can also be written as \(\min_xL(x,u^{*},v^{*})\).</li> <li>Say the optimal solution for this after minimising wrt $x$ is \(\tilde x\) . Note that as of now \(\tilde x \neq x^*\) . That is what we have to enforce but we will get there soon. For now remember that \(\tilde x\) is different and is the optimal solution for the above.</li> <li>If \(\tilde x\) is the optimal solution then if we get the sub-gradient of $L$ at \(\tilde x\), that is \(\partial L(\tilde x,u^{*},v^{*}) = 0\). (Since we are using a convex differentiable function, we can consider that sub-gradient is the same as gradient.). In other words \(0 \in \partial L(\tilde x,u^{*},v^{*})\).</li> <li>Now to back to enforcing that \(\tilde x = x^*\). Only if \(\tilde x = x^*\), the inequality becomes an equality. This is where KKT condition 1 - Stationarity condition comes into play. It enforces this. If \(\tilde x = x^*\) that means like we saw earlier, \(0 \in \partial L(\tilde x = x^{*},u^{*},v^{*})\) which is the same as \(0\in\partial\left(f(x^{*})+\sum\limits_{i=1}^mu^{*}_ih_i(x^{*})+\sum\limits_{j=1}^rv^{*}_jl_j(x^{*})\right)\). We have now shown that both the KKT condition 1 and 2 are required to prove necessity. The conditions 3 and 4 are by default required to enforce feasibility of the primal and the dual problem hence the condition names.</li> </ul> </li> </ul> </li> </ul> </li> </ul> </li> </ul> <p>Hence necessity is proved.</p> <hr/> <h2 id="sufficiency">Sufficiency</h2> <hr/>]]></content><author><name>Rahul Vigneswaran</name></author><category term="distill"/><category term="formatting"/><summary type="html"><![CDATA[I try to undertand and prove KKT conditions]]></summary></entry></feed>